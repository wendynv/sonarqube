sonarqube {
  properties {
    property 'sonar.projectName', 'SonarQube :: Check API'
  }
}

dependencies {
  compileOnly 'com.google.code.findbugs:jsr305'

  testCompile 'junit:junit'
}

artifactoryPublish.skip = true
